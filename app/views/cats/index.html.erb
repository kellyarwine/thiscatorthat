<h1>Which cat is cuter?</h1>
<p>You be the...er...cat-picker.</p>
<br/>
<br/>
<table>
  <tr>
    <td><h1><%= link_to "This Cat", cat_yes_path(@cat2.id, @cat1.id)%></h1>
    </td>
    <td><h1><%= link_to "That Cat", cat_yes_path(@cat1.id, @cat2.id)%></h1>
    </td>
</tr>
  <tr>
  	<td>
    <%= link_to image_tag(@cat2.picture.url, :size => "600x600"), cat_yes_path(@cat2.id, @cat1.id) %>
    </td>
    <td>
    <%= link_to image_tag(@cat1.picture.url, :size => "600x600"), cat_yes_path(@cat1.id, @cat2.id) %>
    </td>
</tr>
</table>
<br/><br/>
<h2>Top Cat Leaderboard:</h2>
<ol>
<% @top_cat.each do |cat| %>
<table>
<tr><td><%= image_tag cat.picture.url, :size => "200x200" %></td>
  <td><%= cat.yesvotes %> Votes</td></tr>
<% end %>
</table>


<br/>


<br/>
<% @cats.each do |cat| %>
<%= link_to (image_tag cat.picture.url, :size => '100x100'), cat %>
<% end %>
<h4><%= link_to "New Cat", new_cat_path%></h4> |